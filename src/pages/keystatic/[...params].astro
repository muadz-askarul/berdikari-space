---
import { getStaticPaths as keystaticGetStaticPaths } from '@keystatic/astro/api'
import Keystatic from '@keystatic/astro/ui'
export const getStaticPaths = keystaticGetStaticPaths

export const prerender = false
---

<Keystatic />
